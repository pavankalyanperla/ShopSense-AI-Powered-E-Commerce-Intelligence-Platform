{
  "permissions": {
    "allow": [
      "Bash(curl -s http://localhost:5100/health)",
      "Bash(curl -s http://localhost:5200/health)",
      "Bash(curl -s http://localhost:5300/health)",
      "Bash(docker-compose up *)",
      "Bash(docker exec *)"
    ]
  }
}
